# Environment variables for Vercel, Netlify, Railway, or self-hosted Node.js
# GATEWAY_URL is required — the relay refuses to start without it.
GATEWAY_URL=https://gateway.ohttp.info/ohttp
MAX_REQUEST_SIZE=1048576
CORS_ORIGIN=*
PORT=3000
